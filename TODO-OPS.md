# 運用TODO (完了したら該当行を消す / 全部済んだらこのファイルごと削除)

## ✅ 済: Supabase 東京リージョン移行の検討 → **見送り** (2026-08-10 ユーザー判断)

GB は **ap-south-1 (ムンバイ)** のまま継続する。**この件を再提案しないこと。**

判断の経緯:
- 2026-08-04「シーズン外になったら案内して」→ 2026-08-10 のシーズン終了時に案内した
- **無料プランは2プロジェクトまで**で、本家PAD (東京・2026-08 作成済み) と GB で枠が埋まっている。
  GB の移行には既存プロジェクトの一時停止/削除が必要で、移行中は GB が停止する
- 遅延 (+130ms/往復) の実害は小さく、1,433人が問題なく利用できた実績がある。
  **書き込みが多く遅延の影響が大きい本家は既に東京**なので、費用対効果で GB は据え置き
- リージョンは利用者から見えない (ホスト名に地域情報なし)。保存データは端末UUID・ダメージ・
  SLv・キャラ選択のみで氏名/メール/認証情報を持たないため、データ所在地の論点も立たない

再検討するとしたら「Pro プランに上げる」「本家を無料枠から外す」等で枠が空いたとき。
以下は当時作った手順 (必要になったら使う):
1. Supabase で新プロジェクト作成 — リージョン **Tokyo (ap-northeast-1)**
2. SQL Editor で `supabase/01〜09` を番号順に適用 → `99_check_applied.sql` で全行 applied を確認
3. `node scripts/gen-seed.mjs` → `supabase/seed.local.sql` を SQL Editor で実行 (slv_ratio/基準)
4. `score_bounds` の運用値を再設定: `update public.score_bounds set min_score=0.1, max_score=2.5 where season='<現行>';`
5. `js/backend.js` の URL / publishable キーを新プロジェクトに差し替え → テスト/E2E → commit → push
6. 旧プロジェクト (ムンバイ) は数日並走させてから Pause/削除 (巻き戻し保険)
※ 過去シーズンの測定データを持ち越したい場合のみ: 旧 SQL Editor で measurements を
   CSV エクスポート → 新プロジェクトへ import (client_id 等はそのまま入る)

## ✅ 済: 見え方確認用の仮データ (2026-07-30 投入 → 2026-08 のシーズン切替で消滅)

シーズン切替の `delete from public.measurements` で仮データ (client_id が
`dddddddd-dddd-4ddd-8ddd-%`) も一緒に消えた。2026-08-10 時点で 2026-07 の利用者は 0 で確認済み。
再度使うときは `node scripts/seed-demo.mjs --from 0 --to 350` (同範囲の再実行は同じデータ)。
消すときは `delete from public.measurements where client_id::text like 'dddddddd-dddd-4ddd-8ddd-%';`

## 🖼️ キャラ画像を BlablaLINK 図鑑アイコンに刷新 (2026-08-01)

ユーザー収集の図鑑アイコン181枚 (透過128px・resource_id付き) を組み込み。
**画像あり 67→180キャラ** (BlablaLINK 178 + 本家OCRフォールバック 2)。日本語名運用は不変
(ビルド専用の data/blabla-map.json で resource_id→日本語名を対応付け)。
- 画像はあるがマスタに無い: Frima(142) / Brid素体(70) / Soline素体(71) — 本家DBに登録されたら
  blabla-map に追記するだけで画像が付く
- マスタにあるが画像なし (未所持等・自作タイル表示): A2/エミリア/シフティー/シュエン/
  ジル・バレンタイン/パワー/プリム/マキマ/ミカ(+スノーバディ)/ラム/姫野/テラ:クリリ +
  スキン5種 (サクラ:ブルーム/ネオン:ブルーオーシャン/マクスウェル:オーディナリー/
  ラプラス:アルティメット/ルピー:ウィンターショッパー)
- ~~テラ:クリリ~~ → **OCRゴーストと判明** (2026-07-31 23:07 JST に観測1回・未確認・情報ゼロの
  自動登録行)。GB ビルドに除外フィルタを実装済み (未確認+観測≤1+情報ゼロはスキップ)。
  本家DBの行削除は任意 (設定→キャラ管理から。残っていてもGBには入らない)

## 🧪 技術負債メモ (Codexレビュー 2026-08-01)

- (2026-08-04 追記) 匿名 write RPC (submit / own-edits) にレート制限が無い —
  Supabase 側の設定か Edge Function 化で絞れるが、現状は shadow 集計 +
  「correct は既存行のみ」ガードで実害を抑えている。荒らしの実弾が観測されたら着手

- build-characters.mjs の取得ロジック (blabla優先/ゴースト除外/sticky ID/改名警告) に
  回帰テストが無い (現テストは characters.json スナップショット整合のみ)。
  DB fetch をモック注入できる形に切ってからテスト追加するのが筋 — 余裕のある時に

## 🔧 マクスウェルは2キャラ (2026-08-01 訂正)

- **マクスウェル** = 従来キャラ・**B3 鉄甲**アタッカー。本家DBに行が無い (ユニオンで未使用/OCR未検出) ため
  GBマスタにも入らない。図鑑アイコン (rid=102) と旧OCR画像は正しくこちらに紐付け済み —
  本家DBに登録されれば自動で画像が付く
- **マクスウェル：オーディナリーメカニック** = 最新実装の別キャラ・**B2 風圧**。図鑑アイコン未収集のため
  現状は自作タイル表示。BlablaLINK の図鑑から取得できたら assets/blabla-icons/<rid>.webp +
  blabla-map.json に追記で反映される
- 本家OCRの「マクスウェルオーディナリー」(観測9回) は新キャラの読み取り → _name_aliases で正式名に名寄せ

## ⚠️ SLv1000超が測定できない (2026-08-01 判明・要対応)

SLv補正テーブル (めいでん+ふるりの実測) が **1〜1000 まで**しかなく、
ゲーム内の超上位帯 (SLv1000到達済み) が測定できない。
- 現状の扱い: 上限超は入力時点で丁寧な案内を出して測定させない (誤った数値を出さない)
  — js/app.js の SLV_MAX。サーバー側も 01_schema の CHECK と slv_ratio が 1〜1000
- **次の対応**: めいでんさんに SLv1000超の実測値を依頼 → 揃ったら
  ① slv-ratio.json 追記 → ② gen-seed で seed 再生成 → SQL Editor 実行
  ③ 01_schema の CHECK 上限を拡張 (要マイグレーション) ④ SLV_MAX と input[max] を更新
- 参考 (外挿の手がかり): 末尾は +842/843 でほぼ一定、20レベルごとに段差
  (841/861/…/981)、段差の値は約1.068倍ずつ等比増加。ただし**推定値は入れない方針**

## 📍 引き継ぎメモ (2026-07-31 職場PC作業終了時点)

- **現在地**: main = c94ab12・全コミット監査済み (Codex 併用)・CI green・デプロイ済み。
  Supabase は 01〜08 全適用 + score_bounds [0.1, 2.5]。テスト: unit 41 + E2E 14 全パス。
- **2026-07-31 (職場PC) に入った主な変更**:
  - **総合の統計監査 (Codex)**: 現行の「各凸の中央値比の算術平均」は数学的に正当と結論
    (k係数の約分は厳密・SLv補正は行単位で実施済み・ボス補正は中央値除算に内蔵)。式は変更なし、
    誤読防止の文言のみ厳密化 (「合計ダメージの比ではありません」明記)。
  - **結果画面に tips**「❓ この%はどう計算している?」(折りたたみ) — SLv散布図の
    「真ん中の人の曲線=100%」の言い方で属性%/編成内%/総合を先回り開示。
  - **編成の見える化**: 結果カードとシェアカード v6 (canvas 1200x800) の両方に
    「使った編成 (5人タイル・バースト順表示 = tiles.js sortForDisplay)」+
    「編成内% (同一編成の中央値比・15人未満は解禁案内)」。結果カードの序列は
    シェアカードと同じ「属性%大 → 編成タイル → 編成内% 26px → 分布」(実機FBで昇格)。
    3凸総合は編成補正なしのまま (誰でもどこか1凸で強サポを使えるため — ユーザー判断)。
  - **OGP刷新**: リンクプレビューを「しりすこPAD GB / Developed by 推しりをすこれ部 (ロゴ)」に。
    og:title 変更 + og:image ?v=2 でクローラキャッシュ破り。
    ※ 過去に貼った場所は旧パネルが残ることあり — 新規投稿で確認する。
- **編成内%の実機確認レシピ** (仮データのTOP編成と完全一致で提出すると解禁済み分布が見える):
  灼熱=プリセット#1 (69票) / 水冷=#2 (98票) / 鉄甲=#1 (69票) / 風圧=#3 (66票) /
  電撃=手動 アニス:スター/クラウン/シンデレラ/アニス:スパークリングサマー/メイデン:アイスローズ (80票)。
  職場PCで水冷×同一編成70人=160% の表示は確認済み。
- **2026-07-30 夜 (自宅Mac) に入った主な変更**:
  送信中オーバーレイ (メンバー作 Now Loading GIF・最低1秒・多重送信ガード・背面inert) /
  結果カードの主従をシェアカードと統一 (中央値比%主役・ふるり値サブ・基準値ピル撤去) /
  シェアカードに実ダメージ◯◯B と単発SLv 追加 / シェアカード右上にユニオンロゴ
  (メンバー作バナーから背景透過抽出 — scripts/extract-union-logo.py で再生成可) /
  トップに募集バナー掲載 + 文言正式化 (ユニオン「推しりをすこれ部」・ダイヤ帯目標) /
  stats 編成ランキング大タイル化 / GIF 1.5倍減速。
  アセット出典: 募集バナー原本 = 自宅Mac ~/Desktop/IMG_9745.JPG (リポジトリに同内容の
  assets/recruit-banner.jpg あり)・GIF原本 = ~/Downloads/IMG_0385.GIF。
- **別PCでの最初の一歩**: 両リポジトリを同じ親フォルダに pull → GB の CLAUDE.md →
  `node tests/run-tests.mjs`。Codex フラグは PC ごと: 本家側で `touch .claude/hooks/.codex-on`。
  **作業開始時は必ず git fetch で別PC分を確認**。
- **決定済み (2026-07-30)**: Codex 運用は2レーン化せず**基本併用のまま** (外すときはユーザーが都度指示)。
- **未決の判断事項** (ユーザー判断待ち):
  1. IDEAS-pad-integration.md (本家活用6案) の着手順 — 本家 ROADMAP への転記もまだ
- **実機確認まだ**:
  - 新レイアウトの結果カード (編成内% 26px 昇格 = c94ab12) と シェアカード v6 (編成タイル+編成内%列)
  - iOS 自由枠ピッカーの重なり修正 (c6fe567/ee7b3e0) — まだ重なるならスクショ+iOSバージョンを聞く
  - 07-30 自宅Mac分 (シェアカード右上ロゴ・送信中GIF・募集バナー)
  - 新OGPパネル (Xに新規で貼って確認)
- **✉️ 公式への画像使用許諾: 二次返信で決着 (2026-08-10「詳細は二次創作ガイドラインを参照」
  = 個別許諾は出ない)** → **キャラ画像の掲載を停止** (ガイドライン第1条4項)。
  権利元 SHIFT UP CORP. の窓口 `business@shiftup.co.kr` (第2条5項が指定) へ
  **2026-08-10 に利用許諾を申請するメールを送信済み — 返信待ち**。
  許諾が出るまで `USE_CHAR_IMAGES` を true に戻さないこと (CIのテストが false 固定を担保)。

  照会した内容 (2点):
  1. 本ツールはゲームではなくプレイ記録の補助ツールだが、第3条3項 (ゲームIPを活用した
     ゲームの製作・配布・サービスの禁止) の対象になるか
  2. (1)が問題なければ、編成識別を目的としたキャラクターアイコン画像の表示を許諾いただけるか
     (非営利維持・著作権表示・小サイズ限定・再配布機能なし・要請時は即時停止、を約束)

  **返信が来たときの分岐**:
  - **許諾あり** → README「掲載を再開する手順」に従う (build-characters で画像と hasImg を復元 →
    USE_CHAR_IMAGES=true → 権利方針テスト更新 → sw.js の CACHEABLE と版数を戻す)。
    条件付き許諾なら条件を README の権利方針に明記してから反映する
  - **不許可 / 返信なし** → 現状 (自作タイル) を継続。**催促はしない**
  - **(1)が「対象になる」** → サイトの公開自体を止める判断が必要。ユーザーに即共有すること

  ※ 以下は撤回済みの旧方針の記録:
  - 実装: tiles.js ハイブリッド (USE_CHAR_IMAGES フラグ = 即時撤去レバー)。
    build-characters が本家から画像コピー (67キャラ・使用率TOP20は全属性100%カバー)。
    シェアカードも画像タイル + 最下部に著作権表記
  - 公式から続報が来たら: 許諾条件に合わせて調整 / 不許可なら USE_CHAR_IMAGES=false → push (即日)
