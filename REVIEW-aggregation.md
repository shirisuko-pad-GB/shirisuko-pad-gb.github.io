# 集計方法の再精査 (2026-07-29)

公開リリース前の総点検。対象 = 送信 (`submit_measurements`) → スコア計算 (`measurements_compute`)
→ 分布 (`get_distribution`) → 編成集計 (`get_comp_insights`) の全パイプライン
(最終定義: 05_seasons.sql。ただし submit_measurements のみ 07_sanitize_errors.sql が最終)。

## 現行仕様のまとめ (検証済み・変更なし)

| 項目 | 仕様 | 評価 |
|---|---|---|
| スコア計算 | サーバー側トリガのみ (クライアント申告値は無視) | ✅ 改ざん耐性の要。維持 |
| 順位づけ | `norm_damage = damage ÷ slvRatio[slv]`。score は表示用換算 (同一属性内では順位が一致) | ✅ 整合確認済み |
| 1端末1票 | client_id ごと属性ごとベスト1件 (max norm_damage) | ✅ 重複投稿に頑健 |
| シーズン絞り | season 刻印 + active-season ガード + シーズンごと全リセット | ✅ regime 混在を防ぐ |
| 代表値 | 中央値・パーセンタイル (平均不使用)。ヒストグラムは p1〜p99 トリム | ✅ 外れ値に頑健 |
| 解禁ゲート | サーバー強制 (分布50 / 同一編成15 / 編成データ10 / 個別編成開示5) | ✅ 少数特定を防ぐ |

## 今回の変更が集計に与える影響

- **キャラID正規化 (characters.json v2)**: 同一キャラのアイコン違いIDが代表IDに統一されたため、
  **今後の提出は同一編成が正しく同じ comp_key に集まる** (旧仕様では票が割れていた = 改善)。
- **既存データ (2026-07 の提出済み分) は旧IDのまま**: 旧提出と新提出で同じ編成でも comp_key が
  分かれることがある。スコア・属性分布には無影響 (comp_key は編成集計にしか使われない)。
  DB内の編成書き換えマイグレーションは行わない (リスク > 益。シーズンリセットで自然解消)。

## 発見した問題と対処

1. **「直近120日」の説明文が残存** (app.js / stats.html) — シーズン制移行 (05) で時間窓は撤去済み
   なのに文言だけ残っていた → **修正済み (今回のコミット)**。
2. **damage にサーバー側の上限が無い**: 桁ミス (B単位でなく生値をB欄に入れる等) や荒らしの
   極端値は p1-p99 トリムと中央値でほぼ吸収されるが、その端末のベスト票として分布の n に残り続ける
   → **06_input_bounds.sql を新設** (0 < damage < 1000B のCHECK。SQL Editor で適用)。
   ※ 適用しなくても既存動作は変わらない (適用推奨)。

## 提案 (未実装 — 承認待ち)

### A. 中央値比の副表示 (Stage 3 の前倒し・実装小)
分布解禁後 (n≥50)、結果カードに「**中央値比 x.xx** (みんなの真ん中の {median} に対して)」を併記する。
`get_distribution` が既に median を返しているので **DB変更なし・表示だけ**で実装できる。
ふるり値の「基準者の得意不得意が分母に混入する」弱点 (README 記載の既知課題) を、
データが貯まった属性から自然に補正できる。

### B. 3凸総合指標 (Stage 4 = 中央値比の平均)
`set_id` / `set_slot` は収集済み。全5属性が n≥50 になったら、
3凸まとめ送信の結果カードに「総合 = 各凸の中央値比の平均」を表示する (これも表示のみで可)。
例: 総合 1.05 = 平均的プレイヤーの1.05倍。現行の「平均ふるり値」は残す (指標の連続性)。

### C. 将来課題 (現状は受容)
- **Sybil (1人複数端末)**: client_id は自己申告。レート制限・指紋は未実装 (README 記載の既知の限界)。
  n が小さいうちは影響が見えやすいので、不審な分布になったらシーズンリセットで対処。
- **編成補正の回帰モデル (Stage 3 本体)**: 属性ごと数百件たまってから。

## 提案D: 意図的な極端値 (荒らし入力) への多層防御 (2026-07-30 追記)

### 攻撃モデルと現状の耐性

| 攻撃 | 現状の防御 | 残る影響 |
|---|---|---|
| 桁違いの巨大値 (例: 999B) | 06 の CHECK (<1000B) は通ってしまう / 中央値は半ステップしか動かない / ヒスト表示は p1-p99 トリム | **上位%の above に+1** / p99(hi) が伸びる / **編成ランキングの「最高」が汚染される** (max なので1票で決まる) |
| 極小値 (例: 0.001B) | 上位%・中央値ともほぼ無影響 | lo(p1) が僅かに下がる程度 |
| 境界内の吊り上げ (例: 全員 score 3.9 相当) | 固定境界では防げない | 中央値・分布の形が濁る (票数次第) |
| Sybil (client_id を量産して多票) | なし (1端末1票は自己申告) | トリム率固定 (1%等) では票数が閾値を超えると突破される |

### 評価: 「最小・最大の1%棄却」(発案) について
方向は正しいが2つ弱点がある: (1) 荒らし票が1%を超えた瞬間に突破される (固定率は票数に脆い)、
(2) データが綺麗なときは正当な上振れ (本当に強い人) を捨ててしまう。
固定率トリムより **適応的な外れ値フェンス (中央値±k×MAD / IQRフェンス)** が優れる:
汚染が無ければ何も捨てず、汚染があれば量に応じて捨てる (破綻点は理論上50%)。

### Codex 敵対レビュー後の確定結論 (2026-07-30)

初案 (上記L1/L2/L3) を Codex に統計・実装・回避手口の3視点で叩かせた結果、以下に確定。
訂正された事実: 巨大値は above/p99 だけでなく **n・bins・編成の票数にも乗る**。
「p1-p99トリム」は統計量のトリムではなく**ヒストグラム表示域のクリップ** (above/中央値は全票で計算)。
編成「最高」は生 max = 破綻点0% (境界内の1票で支配可能)。IQRフェンスの破綻点は25%であり50%ではない。

**出荷した (08_shadow_stats.sql — 2026-07-30 実装)**:
1. **シャドウ集計 (運営判断で入口拒否から変更)** — 荒らしを**拒否しない**。送信は受理して
   スコアも普通に返し (突っぱねると意地になってエスカレートするため)、シーズン別の妥当範囲
   (`score_bounds`、既定 [0.01, 5.0]) の外の票を**公開集計から黙って除外**する。
   除外は per-client ベスト選抜の**前** (荒らし票が本人の正当票を隠さないように)。
   解禁ゲートは除外後の票数で判定。範囲はシーズン切替ランブックで見直す。
   ※ トレードオフ: リポジトリが公開のため、この仕組み自体は読める人には読める
   (境界値の実運用値は DB のみにあり非公開)。
2. **編成「最高」の廃止** — 生 max は境界内の1票で乗っ取れるため公開をやめ、
   かわりに「**中央値が高い編成TOP3**」(採用5人以上のみ) を返す。
3. **並び順の内訳** — 順不同5人 (comp_key) の使用率順位はそのまま、
   横展開として配置 (characters 配列の完全一致) の内訳上位3を返す。

**保留 (条件付きで再検討)**:
- **L2 (適応フェンス)**: 初案の IQR 版は不採用。導入するなら raw_n≥100 で
  `log(score)` の median±7×MAD・スナップショット基準・除外は公開統計のみ
  (本人には my_outlier を返す)。現状の n では時期尚早 — n が3桁に乗ってから。
- **L3 (IP制限)**: 不採用。日本はモバイルCGNATで正当ユーザーが同一IPを共有するため
  日次件数のhard rejectは誤爆リスク大。実害が出たら Edge/WAF の短時間レート制限+CAPTCHA を先に。
- **固定1%トリム (発案)**: 主防御には不採用 (n=50では0.5票で丸め依存・汚染1%超で突破・
  綺麗なデータでは正当な尾部を捨てる)。表示クリップとしては現行 p1-p99 が同役割を担い済み。

大前提として: **統計的ロバスト性は Sybil 耐性ではない**。値の異常は L1 で殺せるが、
「もっともらしい値の量産」は投稿コスト (認証・CAPTCHA等) でしか防げない — これは実害を見てから。

— Claude (Codex によるダブルチェック済み。指摘対応履歴はコミットログ参照)
