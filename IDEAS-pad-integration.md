# GB データの本家しりすこPAD活用アイデア (2026-07-29 起草)

GB で集まる「属性 × SLv × ダメージ × 編成5体 × シーズン」の匿名集計を、
本家 (shirisu-pad) 側で活かす候補。**基本方針: GB→本家の一方向エクスポートのみ**
(集計済みJSONを本家が読む。DB同士は直結しない — セキュリティ分離を維持)。
公開/非公開の整理: **集計データの公開は提出インセンティブのエンジンなので維持**。
ユニオンの独占的恩恵は「GB集計 × 本家内部データの掛け算」(外部からは構造的に不可能) で出す。

## アイデア一覧 (優先度順)

### 1. ふるり値の「物差し交換」 (世間中央値基準)
- 本家の比較タブ・模擬タブに「世間比」(自分 ÷ GB集団中央値) を併記。
  基準者ふるり個人の得意不得意が分母に混入する既知の弱点を、外部の物差しで補正。
- 必要データ: 属性ごとの中央値 (n≥50 で解禁済みのもの)。GB の get_distribution が返す median をそのまま使える。
- GB側の対応 (REVIEW-aggregation.md 提案A) と同じ軸。

### 2. 最適凸プランソルバーの入力補完 ★実利最大
- player_damages が未登録の属性を「SLv換算した世間中央値 (編成類似があれば編成中央値)」で推定し、
  ソルバーの割当精度を上げる。推定値は UI 上で「推定」と明示。
- 必要データ: 属性別中央値 + 編成別中央値 (n≥5開示分)。SLv換算は本家側の slv-ratio で行う (秘匿維持)。

### 3. 編成ベンチマーク表示
- 本家の模擬タブ/編成ピッカーに「この編成の世間中央値 ◯B相当 / 最高 ◯B」「採用率 ◯%」を表示。
  採用率TOP編成を編成プリセット候補として提案。
- 必要データ: get_comp_insights の comps/chars (編成つき n≥10)。

### 4. 採用率メタウォッチ
- 新キャラ実装後の採用率の推移を記録し、本家の編成提案の鮮度判断に使う。
  変化が大きいときは運営向け Push 通知も可能 (本家に通知基盤あり)。
- 必要データ: 採用率スナップショットの時系列 (エクスポート時に日付を刻む)。

### 5. SLv補正カーブの実測検証 ★運営内限定
- 同一編成・同一ボスで SLv が異なる提出から、slv-ratio カーブを統計的に追試・補間。
  高SLv帯の外挿が改善すればふるり値・ソルバー両方の精度が上がる。
- ⚠ 元データ (slv-ratio) は未公開の検証データ。**再推定カーブも公開しない** (実質的な流出になるため)。
  分析・成果は運営内に留める。

### 6. メンバー個人の育成診断
- メンバーの属性別ふるり値を世間分布に重ね、「どの属性が伸びしろか」をホームタブに表示。
- 必要データ: 属性別分布 (bins/median)。本家側でメンバーのスコア計算が必要
  (本家は slv-ratio をローカルに持つので計算可能)。

## 実装の入れ物 (共通)
- GB 側に `scripts/export-benchmarks.mjs` (新設候補): RPC から解禁済み集計を取得して
  `data/benchmarks.json` を生成 → commit で公開。本家はこの静的JSONを fetch するだけ。
- 解禁していない属性は JSON に含めない (ゲートのサーバー強制と整合)。
- 個人特定につながる生データは RPC 設計上そもそも取得不可 (現状の分離を保つ)。

## 前提となる規模感
| 使えるようになる機能 | 必要な提出数 |
|---|---|
| 1・6 (中央値基準) | 属性ごと 50人 |
| 3 (編成ベンチ) | 編成つき 10人〜 / 編成ごと 5人 |
| 4 (採用率) | 編成つき 10人〜 |
| 5 (SLvカーブ) | 同一編成×SLv分散で数百件規模 |
| キャラ寄与回帰 (GB Stage 3) | 属性ごと数百件 |
